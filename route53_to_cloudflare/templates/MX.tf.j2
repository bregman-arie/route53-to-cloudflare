resource "cloudflare_record" "MX_{{ terrafromResource  }}" {
  name     = "{{ name }}"
  ttl      = "{{ ttl }}"
  type     = "MX"
  priority = "{{ priority1 }}"
  value    = "{{ value1 }}"
  zone_id  = cloudflare_zone.{{ zone_id  }}.id
}


